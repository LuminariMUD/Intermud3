# Historical protocol-document edits

This archive formerly reproduced a short 2023-2024 change list associated with
the upstream I3 proposal. It was not a release history for this gateway and
included version terminology that predates the gateway's deployed
`startup-req-3` path.

The original text remains available in Git history. Use the
[protocol reference index](../intermud3_docs/README.md) and its linked primary
sources for current protocol context.
